# =============================================================================
# .rc — alias / 函数 / 提示符（从 .rc.example 复制并自定义）
# =============================================================================

alias ls='ls --color=auto'
alias grep='grep --color=auto'

# 其他别名、函数在 ~/.rc 中直接添加
